#include <iostream>
using namespace std;
int main()
{
   int length,width,height,total;
   cout<<"enter the length value:";
   cin>>length;
   cout<<"enter the width value:";
   cin>>width;
   cout<<"enter the height value:";
   cin>>height;
   total=length*width*height;
   cout<<"the volume of cuboid:";
   cout<<total;
    return 0;
}
